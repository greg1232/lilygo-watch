#pragma once
// Copy this file to `wifi_secrets.h` and fill in your network details.
// `wifi_secrets.h` is gitignored so credentials never get committed.

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-password"
